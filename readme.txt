// Used to increase the tab index on gravity forms to something higher than anything on the page should be.
// common issue with other forms on the page or even the admin bar being the next thing you tab to after the first input box.
// This fixes it by pasting the code into your functions.php file
